// Code your solution here

// use FILTER to return matching results

// Drivers whose names start with "_"
drivers.filter(function (string) {
    return string == drivers.charAt(0)
})
function findMatching(drivers, string) {
    drivers.filter(function (string) {
        return string == drivers.charAt(0)
    })}


function fuzzyMatch(array, string) {
    return //drivers whose names begin with _
}

function matchName(array, string) {
    return //each element whose name property matches provided string argument//
}